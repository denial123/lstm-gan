rnn.py and gan.py contain neural networks for reading midi files, generating music sequences and saving them as midi files
midi_statistics.py and music_data_utils.py create gnuplot statistics in an output file. Make sure to create folder "plots" in the directory before.
surprise.ipynb is a Jupyther Notebook to calculate Bayesian surprise on the generated sequences.
